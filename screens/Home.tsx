import { View, Text, SafeAreaView } from "react-native";
import Header from "../components/home/Header";

const Home = () => {
  return (
    <SafeAreaView>
      <Header />
    </SafeAreaView>
  );
};

export default Home;
